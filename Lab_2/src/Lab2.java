public class Province {
    private String name;                    // "British Columbia"
    private String capital;                 // "Victoria"
    private int populationInMillions;       // 4 or 38
}

public Province{
        if(isValidPopulation(populationInMillions) && (isValidProvince(province) &&
        (isValidCapitalCity(capital)){
        this.populationInMillions = 0 and 38;
        this.name = name;
        this.capital = capital;
        } else {
        this.populationInMillions = 4;
        this.name = "B.C";
        this.capital = "Victoria"
        }
        }